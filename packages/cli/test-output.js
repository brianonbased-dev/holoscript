// Generated by HoloScript Compiler
// Target: Three.js
import * as THREE from 'three';

const scene = new THREE.Scene();
const camera = new THREE.PerspectiveCamera(75, window.innerWidth / window.innerHeight, 0.1, 1000);
const renderer = new THREE.WebGLRenderer();

renderer.setSize(window.innerWidth, window.innerHeight);
document.body.appendChild(renderer.domElement);

// greeting
const greeting_geometry = new THREE.SphereGeometry(0.5, 32, 32);
const greeting_material = new THREE.MeshStandardMaterial({ color: '#ffffff' });
const greeting = new THREE.Mesh(greeting_geometry, greeting_material);
greeting.position.set(0, 0, 0);
scene.add(greeting);

camera.position.z = 5;

function animate() {
  requestAnimationFrame(animate);
  renderer.render(scene, camera);
}
animate();
